import { NumberAtomicPipe } from './number-atomic.pipe';

describe('NumberAtomicPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberAtomicPipe();
    expect(pipe).toBeTruthy();
  });
});
